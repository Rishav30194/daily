import { expect, test } from 'vitest';

// Placeholder so the deploy workflow's test step is real from the first push.
// The domain tests that matter arrive in phase 1 (grading, carry, dates).
test('the toolchain runs tests', () => {
  expect(true).toBe(true);
});
